import 'dart:async';

Future<String?> mdnsResolveHost(String host, {Duration timeout = const Duration(seconds: 5)}) async => null;

Future<String?> mdnsFindByService({
  String service = '_http._tcp',
  String namePrefix = '',
  String? nameContains,
  Duration timeout = const Duration(seconds: 5),
}) async => null;
